import { RotaBackGroundBodyDirective } from './rota-back-ground-body.directive';

describe('RotaBackGroundBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new RotaBackGroundBodyDirective();
    expect(directive).toBeTruthy();
  });
});
